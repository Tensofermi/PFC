(1)单模_框架：
作为一个简单的PFC的框架，后续的程序都是在此基础性修改得到的。

(2)三模_test：
编写三模的程序，尝试调出不同的样貌

(3)三模_k空间作为初始条件：
直接将k空间的衍射点作为初始条件演化的尝试

(4) 三模data：
存放三模截图、矩阵参量等数据
======================================
辅助调试：
(1)findS
对结果的衍射图（或者f_den）进行分析

(2)fast
为了追求速度而舍弃一些输出和计算

(3)S_q
根据S绘制衍射因子

======================================
三模的完整表示：
N,dx,dt,nsteps——计算精度参数
den0,r,lamda——热力学以及初始参数
km1,km2,km3,b1,b2,b3——几何参数
a——立方项
